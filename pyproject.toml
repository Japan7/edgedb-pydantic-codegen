[tool.poetry]
name = "edgedb-pydantic-codegen"
version = "0.1.0"
description = ""
authors = ["NextFire <git@yuru.moe>"]
readme = "README.md"
packages = [{ include = "edgedb_pydantic_codegen" }]

[tool.poetry.dependencies]
python = "^3.11"
edgedb = "^1.0.0"
pydantic = "~2"
orjson = "^3.8.0"
jinja2 = "^3.1.2"
ruff = "^0.1.2"
isort = "^5.10.1"
autoflake = "^2.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
